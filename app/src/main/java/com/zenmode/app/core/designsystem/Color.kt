package com.zenmode.app.core.designsystem

import androidx.compose.ui.graphics.Color

/**
 * Zen Mode is deliberately monochrome. The palette is the whole design system:
 * anything that is not one of these values needs a reason to exist.
 */
val ZenBlack = Color(0xFF000000)
val ZenSurface = Color(0xFF0A0A0A)
val ZenSurfaceElevated = Color(0xFF141414)
val ZenTextPrimary = Color(0xFFFFFFFF)
val ZenTextSecondary = Color(0xFF8A8A8A)
val ZenDivider = Color(0xFF222222)

/** The single accent, used only for destructive confirmations. */
val ZenAlert = Color(0xFFE05A4F)
