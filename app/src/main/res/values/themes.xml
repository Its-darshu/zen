<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The app is intentionally monochrome and always dark: the Compose theme owns
      all colours. This XML theme only covers the pre-Compose window so there is
      no white flash while the activity starts.
    -->
    <style name="Theme.ZenMode" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
